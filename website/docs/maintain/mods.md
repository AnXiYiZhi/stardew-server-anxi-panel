# Mod 管理

"模组"页支持：

- 手动上传一个或多个 Mod ZIP，也可以在一个 ZIP 中包含多个 Mod 文件夹。
- 搜索 Nexus Mods 并通过浏览器扩展一键安装（含依赖识别：会提示缺失或未启用的前置 Mod）。
- 配置管理员的 Nexus API Key 后，Premium 用户可以走直连安装，无需浏览器扩展。
- 查看每个 Mod 的依赖是否满足、当前存档是否启用，按需启用/禁用（联动同包和前置 Mod）。
- 删除、导出已安装的 Mod。
- 在已安装和按存档配置列表中按名称、文件夹、作者、`UniqueID` 或 Nexus ID 模糊搜索，并按最近安装或名称排序。

::: tip
改动 Mod 后通常需要重启服务器才能生效，面板会在需要时提示"需要重启"。
:::

## 上传与安装时间

- 每个 Mod 根目录都必须有 `manifest.json`；外层可以有任意层目录，Panel 会自动发现其中的 Mod。
- ZIP 中再放 ZIP 不会继续递归解压。请把内层压缩包单独选择上传。
- 手动上传、Nexus Premium 直连和浏览器扩展一键安装都会持久记录安装时间；同一次 ZIP 中发现的多个 Mod 使用同一个时间。
- 列表默认最近安装在前。旧版本已经存在、但升级前没有历史安装时间的 Mod 会排在有记录的 Mod 后面；Panel 不会用文件修改时间冒充安装时间。

## Nexus 相关提示

- Nexus 数字 ID 精确查询需要先在"模组"页配置管理员的 Nexus API Key；未配置时会明确提示缺少 Key，而不是报错 500。普通关键词搜索不需要配置 Key。
- 通过浏览器扩展一键安装 Mod 时，确认扩展已更新到面板提示的最新版本（扩展版本和面板不匹配可能识别不到 Nexus 页面的下载入口）。
- 批量安装多个 Mod（含前置）时，如果某个 Mod 之前已经安装过又被重复提交，面板会自动识别为"已安装，跳过重复导入"，不会算作失败。

## 下一步

- 面板用户与权限：看 [日常维护](/maintain/update)。
- Mod 安装失败排查：看 [常见问题](/faq/)。
- 三个工作台（下载/添加/配置模组）每个按钮的完整教程、玩家同步包详解：看 [深度文档 · Mod 管理](/handbook/mods)。

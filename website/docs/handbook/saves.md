# 存档管理

"存档"页管理服务器当前使用的存档、存档库里的其它存档，以及自动备份策略。

## 首次启动：新建或上传存档

服务器第一次启动、还没有任何存档时，面板会引导你二选一：

### 新建存档

在面板里直接生成一个全新农场，可配置：

- 农场名、玩家名（农民名）。
- 性别。
- 农场类型：**官方农场**包括标准、河边、森林、山顶、荒野、四角、海滩、草原农场共 8 种，每种都有缩略图预览；如果安装了支持自定义农场地图的 Mod（如 Stardew Valley Expanded 的“边境农场”），面板还会扫描并展示模组农场及其依赖状态。只有服务端判定为可选择的项目才能直接创建。
- 初始宠物外观。

::: tip 模组农场什么时候可以选择
新后端默认开启模组农场创建能力，但每张卡仍要通过多重门槛：对应 Mod 已启用、前置依赖就绪、没有冲突、识别结果明确，并且服务端返回“可选择”。未通过的卡片会保持只读并显示原因；旧后端或部署方显式关闭功能时也不会开放选择。
:::

确认后面板会调用 JunimoServer 生成完整世界存档，这一步是同步阻塞的（要生成完整世界才返回），所以点击后需要等待一小段时间，不要重复点击。

### 上传存档

把自己电脑上的 Stardew Valley 存档打包成 ZIP 上传：

1. 拖拽 ZIP 到上传区，或点击选择文件。
2. 点击"预览存档"：面板会解析并展示存档里的农场名、游戏时间、地图类型、玩家名，供你确认这是不是想要的那份存档。
3. 确认无误后点击"导入并启动"完成导入。

上传导入支持来自其它平台（Steam/GOG 等）的存档：如果存档里记录的原平台主机玩家和当前服务器不匹配，面板会引导你选择原主机处理方式（替换平台 ID 或保留原身份），避免盲目接管导致数据丢失。

::: warning 上传/切换存档前必须先停服
存档相关的写操作（新建、切换、删除）都要求服务器当前不在运行/启动中状态，需要先停止服务器。
:::

## 切换 / 管理已有存档

存档库以卡片形式列出所有已导入的存档，每张卡可以：

- **选择**为当前启动存档。
- **导出**为 ZIP（备份到本地，或转移到其它地方）。
- **删除**（删除当前启动存档后，服务器会失去已选择的启动存档，下次启动前需要重新选择/新建/上传）。

## 删除离线玩家角色

服务器**运行中**时，管理员可以在"玩家"页对当前活动存档里的离线玩家角色执行删除：

- 只有满足以下条件的角色才允许删除：当前活动存档中、角色处于离线状态、不是主机角色、已经被玩家认领。
- 如果其他玩家在线，删除操作不会阻止他们继续游戏。
- 删除前面板会强制保存当前进度并创建一份整档保护备份，万一出问题可以随时恢复。
- 删除后该玩家重新上线时服务器会自动中止连接（提示该角色已不存在）。

::: warning 删除操作不可逆
角色被删除后存档里将永久移除该角色及其小屋，虽然面板会提前做保护备份，但仍然建议在操作前确认备份列表里有最近的存档备份。
:::

## 自动备份策略

左下角“自动备份策略”按游戏内日期维护回档点，不按现实时间定时：

- **睡觉存档后创建回档点**：游戏完成一次 `GameLoop.Saved` 写盘后，Panel 自动打包当前游戏日。
- **保留最近 N 个游戏日**：默认保留 5 个，可设置为 1–14 个。超过数量时只清理最早的自动回档点。
- 同一游戏日重复保存会覆盖该日的同一个回档点；跨季节、跨年份仍按游戏内日期连续排序。
- 手动备份和删除、回档、人物删除、导入、运行组件升级前的保护备份不占用这 N 个名额，也不会被自动清理。

从 v0.4.4 起，自动回档由 Panel 后台及时处理，不需要管理员打开“存档”页才能生成。升级前已经缺失的历史游戏日没有旧磁盘内容可供重建，会从升级后的下一次游戏保存开始保持连续。

## 备份列表：恢复与删除

“游戏日回档”按游戏日期展示自动回档点；“其他备份”保存手动和各类保护备份。每个备份支持：

- **回档到此日**：用该备份覆盖当前存档前会弹窗二次确认；服务器运行中可由面板执行受控停止、回档和重启。
- **删除**：清理不需要的旧备份文件。

::: tip 什么时候该手动确认备份
批量安装/删除 Mod 之前、或者做任何可能影响存档的操作之前，建议先手动看一眼备份列表，确认最新一份备份是不是你期望的状态。
:::

## 下一步

- 装 Mod 前记得先看这里的备份策略是否开启：[Mod 管理](/handbook/mods)。
- 存档相关报错（导入失败、预览解析失败等）：看 [常见问题](/faq/)。
